# ai-resume-analyzer
Ai Resume analyzer is an NLP-based tool that takes a resume and a job description as input, extracts keywords using TF-IDF vectorization, and computes a cosine similarity score to measure compatibility
